"use strict";
/**
 * @since 0.1.0
 */
Object.defineProperty(exports, "__esModule", { value: true });
exports.unsafeUnwrapLeft = exports.unsafeUnwrap = void 0;
const E = require("fp-ts/Either");
/**
 * Unwrap the value from within an `Either`, throwing if `Left`.
 *
 * @since 0.1.0
 */
exports.unsafeUnwrap = (x) => {
    // eslint-disable-next-line functional/no-conditional-statement, functional/no-throw-statement
    if (E.isLeft(x))
        throw 'Unsafe attempt to unwrap Either failed';
    return x.right;
};
/**
 * Unwrap the value from within an `Either`, throwing if `Right`.
 *
 * @since 0.5.0
 */
exports.unsafeUnwrapLeft = (x) => {
    // eslint-disable-next-line functional/no-conditional-statement, functional/no-throw-statement
    if (E.isRight(x))
        throw 'Unsafe attempt to unwrap Either failed';
    return x.left;
};

/// <reference types="node" />
import { Provider } from "@ethersproject/providers";
import { DNSSEC } from './typechain/DNSSEC';
import { ProvableAnswer, SignedSet } from '@ensdomains/dnsprovejs';
export declare class OutdatedDataError extends Error {
    answer: SignedSet<any>;
    constructor(answer: SignedSet<any>);
}
export interface RRSetWithSignature {
    rrset: Buffer;
    sig: Buffer;
}
export declare class Oracle {
    contract: DNSSEC;
    now: () => number;
    constructor(address: string, provider: Provider, now?: (() => number | undefined));
    getProofData(answer: ProvableAnswer<any>): Promise<{
        rrsets: RRSetWithSignature[];
        proof: Buffer;
    }>;
    private knownProof;
    private encodeProofs;
}
